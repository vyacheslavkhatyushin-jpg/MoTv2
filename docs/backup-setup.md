# Автобэкап БД — настройка ключей/доступов на сервере

Расписание, количество хранимых локальных копий и включение/выключение
офсайт-целей настраиваются прямо в **Настройки → Резервное копирование**
(доступно только `admin`). Но сами учётные данные для доступа к офсайт-целям
(SSH-ключ, сервисный аккаунт Google) в веб-интерфейсе не хранятся и не
вводятся — это осознанно: секреты живут только на сервере, в файлах и
переменных окружения. Эта инструкция — как их туда положить, один раз, при
первой настройке.

Если ни один из офсайт-вариантов ниже не настроен — автобэкап всё равно
работает и делает **локальные** копии БД (см. `server/data/backups/`,
ротация по `local_retention_count`). Офсайт — это дополнительная защита на
случай, если сгорит сам сервер целиком.

Что нужно сделать один раз на сервере (`/home/a-khatyushinv/mot-git/repo`):

## 1. Другой сервер по SSH

Нужен: SSH-доступ до другого сервера, куда будут копироваться файлы бэкапа.

1. Сгенерировать отдельную пару SSH-ключей **специально под бэкап** (не
   переиспользуйте свой личный ключ):

   ```
   ssh-keygen -t ed25519 -f ./secrets/backup_ssh_key -N "" -C "mot-backup"
   ```

   Это создаст `secrets/backup_ssh_key` (приватный, остаётся на этом
   сервере) и `secrets/backup_ssh_key.pub` (публичный).

2. Скопировать публичный ключ на сервер-приёмник и разрешить им заходить
   под каким-нибудь пользователем (можно завести отдельного, узкого по
   правам — доступ ему нужен только на запись в одну папку):

   ```
   ssh-copy-id -i ./secrets/backup_ssh_key.pub <user>@<backup-host>
   ```

   Или вручную дописать содержимое `.pub`-файла в
   `~/.ssh/authorized_keys` пользователя `<user>` на `<backup-host>`.

3. На сервере-приёмнике создать папку под бэкапы, например:

   ```
   ssh <user>@<backup-host> "mkdir -p /home/<user>/mot-backups"
   ```

4. В `.env` рядом с `docker-compose.yml` (создать файл, если его ещё нет)
   добавить:

   ```
   BACKUP_SSH_HOST=<backup-host>
   BACKUP_SSH_USER=<user>
   BACKUP_SSH_PORT=22
   BACKUP_SSH_REMOTE_DIR=/home/<user>/mot-backups
   ```

5. В `docker-compose.yml` раскомментировать две строки с
   `backup_ssh_key` — по одной у сервисов `mine-ops-tool` и
   `backup-worker` (это один и тот же файл, монтируется в оба места,
   каждому нужен доступ на запуск бэкапа: `mine-ops-tool` — по кнопке
   «Сделать бэкап сейчас», `backup-worker` — по расписанию):

   ```yaml
   - ./secrets/backup_ssh_key:/run/secrets/backup_ssh_key:ro
   ```

6. Пересобрать и перезапустить оба сервиса (см. раздел «Деплой» ниже).

Проверка: в Настройки → Резервное копирование бейдж у «Другой сервер по
SSH» должен смениться с «не настроено на сервере» на «настроено» —
после этого чекбокс включения станет доступен.

## 2. Google Drive

Важно: если ваш Google-аккаунт — обычный **личный Gmail** (не корпоративный
Google Workspace), сервисные аккаунты тут не подходят — у них нет своей
квоты хранилища, и они не могут владеть файлами в обычной папке личного
Диска (это работает только в Shared Drive, а это функция Workspace).
Поэтому здесь — OAuth-авторизация от имени вашего реального аккаунта,
через **Device Flow**: код вводится один раз на любом устройстве с
браузером, дальше сервер работает по refresh-token без повторного входа.

1. Зайти в [Google Cloud Console](https://console.cloud.google.com/),
   создать проект (или использовать существующий).
2. Включить **Google Drive API**: APIs & Services → Library → найти
   "Google Drive API" → Enable.
3. Настроить экран согласия (если ещё не настроен): APIs & Services →
   OAuth consent screen → User Type **External** → заполнить название
   приложения и свой email → на шаге Test users добавить свой же Google
   email (пока приложение не прошло верификацию у Google, входить могут
   только явно добавленные тестовые пользователи). Важно: пока статус
   приложения — **Testing**, refresh-token протухает через 7 дней, и
   бэкап на Google Drive начнёт падать раз в неделю. Чтобы этого
   избежать, на странице OAuth consent screen нажмите **Publish app**
   (перевод в статус **In production**) — для scope `drive.file`
   (доступ только к файлам, созданным самим приложением, не ко всему
   Диску) верификация у Google не требуется, это чисто переключатель
   статуса, без дополнительных проверок.
4. Создать OAuth-клиент: APIs & Services → Credentials → Create
   Credentials → OAuth client ID → тип приложения **TVs and Limited Input
   devices**. Получите **Client ID** и **Client Secret** — они не такие
   чувствительные, как приватный ключ, но всё равно держите в `.env`,
   не в git.
5. В `.env` рядом с `docker-compose.yml` добавить:

   ```
   BACKUP_GDRIVE_CLIENT_ID=<Client ID>
   BACKUP_GDRIVE_CLIENT_SECRET=<Client Secret>
   ```

6. Получить refresh-token (это и есть постоянный "пароль" для бэкапа) —
   на сервере:

   ```
   cd /home/a-khatyushinv/mot-git/repo
   docker compose run --rm -e BACKUP_GDRIVE_CLIENT_ID=<Client ID> \
     -e BACKUP_GDRIVE_CLIENT_SECRET=<Client Secret> \
     mine-ops-tool node backup/gdrive-device-auth.js
   ```

   Скрипт выведет ссылку и короткий код — откройте ссылку на телефоне
   или своём компьютере, войдите под своим Google-аккаунтом (тем самым,
   на чьём Диске будет храниться папка с бэкапами), введите код,
   разрешите доступ. Скрипт сам дождётся подтверждения и выведет строку
   вида `BACKUP_GDRIVE_REFRESH_TOKEN=...` — допишите её в `.env`.

7. Создать папку на Google Диске под своим обычным аккаунтом (тем же,
   которым авторизовались на предыдущем шаге), например «MoT backups».
   Расшаривать её никуда не нужно — токен уже даёт доступ от вашего
   имени, папка и так ваша.
8. Взять ID этой папки — часть URL после `/folders/`, когда папка
   открыта в браузере:

   ```
   https://drive.google.com/drive/folders/<ЭТОТ_ID>
   ```

9. В `.env` добавить:

   ```
   BACKUP_GDRIVE_FOLDER_ID=<ЭТОТ_ID>
   ```

10. Пересобрать и перезапустить оба сервиса (см. раздел «Деплой» ниже) —
    файловых секретов для Google Drive монтировать не нужно, все четыре
    переменные (`CLIENT_ID`/`CLIENT_SECRET`/`REFRESH_TOKEN`/`FOLDER_ID`)
    читаются прямо из `.env`.

Проверка: бейдж «Google Drive» в Настройках должен смениться на
«настроено».

Если позже переедете на корпоративный Google Workspace — там есть Shared
Drives, и можно будет сделать вариант через сервисный аккаунт (без
интерактивного входа); текущий OAuth-вариант тоже продолжит работать.

## 3. S3 (пока не реализовано)

Целевой модуль под S3 ещё не написан (`server/backup/targets/s3.js` не
существует) — сейчас доступны только SSH и Google Drive выше. Когда
понадобится S3, интерфейс уже рассчитан на добавление третьей цели без
переделки остального: нужен будет только новый файл-таргет с тем же
контрактом (`isConfigured()` / `upload()`), что и у `ssh.js`/`gdrive.js`,
плюс аналогичные переменные окружения (`BACKUP_S3_*`) и пункт в этом же
разделе Настроек.

## Где что лежит — шпаргалка

| Что | Где |
|---|---|
| Приватный SSH-ключ | `./secrets/backup_ssh_key` (на хосте, никогда не в git — см. `.gitignore`) |
| Google Drive: client id/secret/refresh-token/folder id | `.env` рядом с `docker-compose.yml` (никогда не в git) |
| Не-секретные настройки (host/user/remote dir) | `.env` рядом с `docker-compose.yml` |
| Расписание, ретеншн, вкл/выкл целей | Настройки → Резервное копирование (хранится в БД, таблица `backup_settings`) |
| Локальные копии БД | `server/data/backups/` внутри volume `mine-ops-data` |
| История запусков | Настройки → Резервное копирование → «История» (таблица `backup_runs`) |

## Деплой после добавления/смены ключей

```
cd /home/a-khatyushinv/mot-git/repo
git pull origin main
docker compose up -d --build mine-ops-tool backup-worker
```

Оба сервиса нужно пересобрать/перезапустить вместе — им обоим нужны
одни и те же переменные окружения и смонтированные секреты (см. таблицу
выше: `mine-ops-tool` умеет бэкапить по кнопке «Сделать бэкап сейчас»,
`backup-worker` — по расписанию из БД).

## Если что-то пошло не так

- Бейдж в Настройках так и показывает «не настроено на сервере» после
  раскомментирования volume и пересборки — скорее всего опечатка в пути
  файла в `.env`/`docker-compose.yml`, либо контейнер не пересобрался
  (проверьте `docker compose ps`, `docker compose logs mine-ops-tool`).
- «Сделать бэкап сейчас» падает с ошибкой чтения ключа SSH — самая частая
  причина: файл `./secrets/backup_ssh_key` не существовал на хосте **в
  момент первого запуска контейнера** с раскомментированной строкой
  volume — Docker в этом случае молча создаёт на его месте пустую
  **папку**, а не файл, и чтение падает с непонятной ошибкой. Решение:
  остановить сервисы, убедиться что файл реально существует и не
  является папкой (`ls -la ./secrets/`), при необходимости удалить
  случайно созданную папку и положить файл заново, затем поднять
  сервисы снова.
- Бэкап на Google Drive падает с ошибкой вида "Service Accounts do not
  have storage quota" — значит используется старый способ через
  сервисный аккаунт на личном (не Workspace) Google-аккаунте; нужно
  перейти на OAuth-способ выше (`gdrive-device-auth.js`).
- Бэкап на Google Drive падает с `invalid_grant` — обычно означает, что
  refresh-token отозван (доступ приложению отозвали вручную в настройках
  Google-аккаунта, либо OAuth consent screen в статусе Testing и токен
  протух через 7 дней неактивности) — получите новый токен через
  `gdrive-device-auth.js` заново.
